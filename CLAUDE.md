# claude-codex-orchestrator-plugin

## Agent skills

### Issue tracker

Issues live as GitHub issues in `aliyusufergin/claude-codex-orchestrator-plugin`, managed via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

Default five-role vocabulary — label string equals role name. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context — `CONTEXT.md` + `docs/adr/` at the repo root. See `docs/agents/domain.md`.
