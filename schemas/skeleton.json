{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Skeleton delegation result",
  "description": "Provisional output schema for the walking skeleton. One schema per Delegation Class - Advisory and Verifiable - replaces it once Task Kinds exist.",
  "type": "object",
  "additionalProperties": false,
  "required": ["summary"],
  "properties": {
    "summary": {
      "type": "string",
      "minLength": 1,
      "description": "What the Worker did or found."
    }
  }
}
